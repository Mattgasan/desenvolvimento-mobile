import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: '',
    justifyContent: 'center',
    marginBottom: '5',
  },
});

export {styles}
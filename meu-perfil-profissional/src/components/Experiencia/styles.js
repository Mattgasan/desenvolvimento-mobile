import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: '',
    justifyContent: 'center',
   paddingBottom: 24,
    paddingLeft: 24,
  },
  paragraph: {
      marginBottom: 5,
    fontSize: 18,
    fontWeight: 'bold',
    color: 'green',
  },
});

export {styles}